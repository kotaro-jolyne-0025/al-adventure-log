-- ==============================================================================
-- V17: 補上前後端皆已支援的 ARTIFACT（神器）稀有度
--
-- IF NOT EXISTS 讓已經手動加過此 enum value 的 Supabase 環境也能安全執行。
-- ==============================================================================

ALTER TYPE item_rarity ADD VALUE IF NOT EXISTS 'ARTIFACT';
