# 文件盤點與衝突紀錄

初次盤點：2026-09-17。最近更新：2026-09-19。狀態：變化式帳本、補登快照、character 累計、戰利品差額同步、兩種已確認來源與 nullable 舊來源已完成實作及自動化驗證；正式資料 migration 與真人裝置驗收仍須在部署流程中執行。

## 2026-09-19 實作驗證紀錄

- Flyway V20 已在測試用 PostgreSQL 17 從空資料庫依序執行 V1–V20，並驗證版本 1、來源與開卡基準回填。
- 後端完整測試 43 項通過；`mvnw clean package -DskipTests` 成功產出 `backend-1.1.3.jar`。
- 前端測試 21 項通過；production build 成功。既有 adventure-form 與 inventory-list 樣式預算仍為 warning，沒有 build error。
- `openspec validate baseline-project-specs --strict --no-interactive` 通過。
- 已用自動化案例覆蓋開卡基準、一般新增、9/1 補登、修改差額、刪除反向入帳、正負魔法物品、部分待補、明細超額、兩種來源、3→5、5→1、只改備註不補貨及交易回滾。
- 尚待真人手動驗收：以實際登入帳號逐項操作上述流程，並在 360px 手機 viewport 確認表單、來源標籤、警告與按鈕沒有水平溢出。完成前不勾選 tasks.md 5.5，也不宣稱正式環境驗收完成。

目前已產生 7 份能力草案、50 個需求及 87 個情境。`openspec validate baseline-project-specs --strict --no-interactive` 通過；這只表示目前內容格式有效，不表示需求覆蓋完整、所有決策已確認或產品測試通過。尚未建立正式設計及 tasks.md；已確認問題的後續修正清單記於下方 C01，尚未同步到 `openspec/specs/` 或歸檔。

本紀錄依工作目錄文件、靜態程式檢查及明確標示的使用者手動測試回報；Agent 沒有連線正式資料庫、執行 migration、測試 OAuth 平台或驗證正式部署。程式存在不等於驗收通過。文件中的「已上線」與測試數字是原作者紀錄，不是本輪重新驗證結果。

## 文件覆蓋與去向

| 已閱讀來源 | 可規格化內容／處理方式 |
| --- | --- |
| `system-requirements-spec.md` 全文 | 功能、欄位、API、UX、安全補充；分入七個能力。§6 效能／可用率待補量測條件；§9.4 與 §2.2.4 衝突 |
| `user-stories.md` 全文 | 驗收案例來源；逐個 Epic 對照。未勾選不直接解讀為未實作 |
| `database-schema.md` 全文 | 資料關聯、刪除方向、歷史 migration；保留技術參考，待與 Flyway V1–V19 完整對照後才可成為最新結構手冊 |
| `README.md` 全文 | 系統目的、功能範圍、開發方式；完成後改作導覽，避免複製所有規格 |
| `dnd-adv-log-plan.md` 全文 | 初期 MVP 計畫；無登入、JPA、旧資料欄位等不能直接當現行需求 |
| `zeabur-deploy-fix-plan.md` 全文 | 特定歷史部署事件；保留歷史記錄，不視為現行 schema 管理流程 |
| `AGENTS.md` 全文 | 工作流程指引；JPA、缺檔連結及規格權責需後續修正 |
| `frontend/README.md`、`backend/HELP.md` 全文 | 產生器／開發工具說明；不轉成產品需求 |
| `.agents/skills/git-guidelines/SKILL.md` | 開發流程，保留為 Git 指引，不轉成產品需求 |
| `openspec/config.yaml` | 已有初始化設定，保留使用者內容；定稿後才調整來源文件責任 |
| `backlog.md`、`.agents/rules/project-conventions.md` | `AGENTS.md` 引用但目前不存在；不假設其內容，也不捏造歷史進度 |

## 規格來源追蹤

| 規格 | 文件依據 | 已交叉檢查的實作 | 仍未覆蓋／受阻內容 |
| --- | --- | --- | --- |
| identity-access | SRS §2.0、US-AUTH-001–005 | AuthService、OAuthService、authGuard、路由 | C11 第三方綁定與停用帳號；完整驗證邊界待補 |
| character-management | SRS §2.1、US-001–004 | CharacterRequest、CharacterService、角色表單、CharacterMapper | C05 玩家名稱、C06 職業契約、C04 時間順序與等級 |
| adventure-log | SRS §2.2、§3.3–3.4、US-005–009 | AdventureEntryService、Request、Controller、冒險表單／列表 | C03–04 變化快照／補登、C06–09 驗證與活動、C14 排序 |
| inventory-provenance | SRS §2.3、US-013–016b、DB 關聯 | updateGainedItem、syncGainedItems、InventoryItemMapper、倉庫列表 | C01–02 同步及計數、C10 快捷消耗、C15 型別與同調 |
| state-consistency | SRS 2026-09-17 補充、§9.3、US-MOB-003 | 完整儲存 DTO／Service、讀取保護、快取與相關測試檔存在 | 回歸測試本輪未重跑；交易回滾尚需資料庫驗證 |
| mobile-pwa | SRS §6、§9.1–9.2、US-017–018、US-MOB-001–002 | ngsw-config.json、現有手機樣式歷史需求 | C12 離線資料、C17 效能／可用性指標；裝置驗收未執行 |
| legal-presentation | SRS §2.6、US-LEG-001–003 | `/legal` 公開路由及相關元件檔 | C16 頁腳入口；既有聲明文字沿用，不作法律有效性判定 |

## 本輪補充決策：刪除冒險
使用者確認「刪除冒險就要把東西都扣一扣記錄進 character」。刪除任何冒險須撤回該筆等級、各職業、金幣及休整期變化；不扣除初始或總計快照。全部成功刪除後回到開卡等級／職業及資源 0。
撤回淨支出是加回資源，不是扣除整筆結束快照。刪除及 character 更新須同一交易，失敗全部回滾。
使用者續確認「沒關係不用阻止」：刪除造成當前金幣或休整期為負數仍允許，照實寫入 character 並由 character-shell 顯示，不歸零、不要求先修正後續支出。此決策不代表放寬等級／職業或倉庫數量限制；等級／職業變化造成不合法的處理仍待討論。戰利品倉庫同步仍依 C01–02 討論，不把歷史獲得數量直接當現存數量扣回。

## 本輪補充決策：只記錄變化
使用者確認冒險不再要求玩家輸入起始或結束狀態；玩家只填本次等級／職業、金幣、休整期及魔法物品變化。系統保存該筆當下初始值、變化值及總計值，詳情以紙本帳本方式呈現。角色當前值由開卡狀態加上所有冒險變化計算；初始及總計快照不重複計入。

## 本輪補充決策：清空後重新開始
使用者確認：所有冒險成功刪除後，保留開卡等級／職業，金幣及休整期歸回 0；之後新增冒險只累加其變化值，不沿用已刪除快照。

## 2026-09-19 補充決策：開卡基準可修正

使用者確認開卡不必限定金幣與休整期為 0；建立角色時可設定起始職業／等級、金幣及休整期，開卡已有魔法物品則記入倉庫。角色-shell 的「編輯角色」入口可修改開卡基準；有冒險時先預覽以新基準加總既有變化後的目前值，經玩家確認才寫入。既有冒險快照與倉庫物品不隨基準修正而改寫；若舊冒險快照本身錯誤，玩家另行逐筆修正。此決策取代本 review 中「建立後資源為 0」及「清空冒險後資源歸 0」的舊規則。

## 待討論的業務衝突

除明確標示使用者已確認的部分外，其餘建議仍待確認。影響產品行為的項目在確認之前不寫成確定規則。

### C01 — 歷史戰利品修改與補貨（核心規則已確認，程式待修正）

- 文件：SRS §2.2.4、US-008 允許修改／刪除；SRS §9.4 卻鎖定既有快照、僅可追加。SRS §2.3.2 要求倉庫消耗／刪除不改歷史。
- 程式：`AdventureEntryService.updateGainedItem` 對消耗品同步差額；找不到倉庫項目時會補建。`syncGainedItems` 每次完整儲存都處理既有項目，故沒有修改獲得數量也可能補回已移除物品。永久物品數量更新又與消耗品不同。
- 初期回饋：使用者表示一般操作符合預期，因此以保留現有體驗為基準重新檢查完整流程。
- 重新核對：前端編輯表單允許既有戰利品增刪修，並完整提交所有戰利品；永久物品每張卡片以一件儲存，消耗品可調整數量。後端對仍存在的消耗品倉庫列，已採 `max(0, 現存數量 + 新獲得數量 - 舊獲得數量)`，歸零則移除。原先建議的正差額範例其實就是現有行為，不應描述為必須新開發的規則。
- 核對範例：歷史 3、倉庫 1，改歷史為 5 → 倉庫 3；倉庫仍存在且只改備註 → 數量不變；從冒險刪除戰利品 → 刪除快照及對應倉庫項目；只從倉庫刪除 → 歷史快照保留。
- 2026-09-18 使用者手動重現：依對話測試流程刪除倉庫藥水後，重新儲存來源冒險，回報「有補回 3 瓶」。這是使用者回報，非 Agent 自行執行的測試；其餘情境尚未因此視為已通過。
- 2026-09-18 已確認決策：保留歷史戰利品可更正；獲得數量未改變時，修改名稱、備註或重存冒險不補回已移除物品；消耗品獲得量 3→5 只增加差額 2（倉庫已移除也僅建立 2）；成功儲存後再存相同快照不重複入庫；真正新增的戰利品正常入庫。
- 舊資料漏入庫的修復另行處理，不作為一般儲存自動補貨的理由；本次不新增消耗歷程系統。
- 規格位置：`specs/inventory-provenance/spec.md` 的 `Editable historical loot`、`Loot corrections preserve inventory consumption`、`New loot enters inventory`。舊 SRS §9.4 的戰利品「只能追加」敘述不作為本次基線；待文件整體收斂時更新原文件。
- 2026-09-18 已確認：歷史獲得數量向下修正時，倉庫依負差額扣減但最低為 0；若負差額大於現存數量，歷史仍正常修正，超出部分視為先前已使用或刪除，不阻止送出、不建立負庫存，也不補建物品。C10 歸零操作及 C15 型別變更仍不包含在本次同意範圍。

後續小範圍修正（未實作）：

- [ ] 修正既有快照找不到倉庫項目時的補建分支：數量未變不補回，消耗品正差額僅入庫差額，保留真正新戰利品入庫及來源驗證。
- [ ] 補回歸案例：倉庫仍存在的差額同步、刪除後僅改備註／名稱、刪除後 3→5、成功後重存相同快照，以及新增戰利品。
- [ ] 完成修正後重新執行使用者的 3 瓶藥水操作流程，確認倉庫不再自動補回且歷史仍保留。

### C02 — 魔法物品數的來源與單位（目前持有數規則已確認）

- 文件：SRS §2.2.1 定義起始魔法物品取上一筆合計；HUD／倉庫異動又要求即時更新。
- 程式：後端 `getDefaults` 取倉庫 `COUNT(*)`；但前端 `AdventureFormComponent.loadDefaults` 另讀倉庫，加總永久物品 `quantity` 並覆寫表單起始物品數，與 HUD 使用相同算法。一般倉庫讀取成功時，數量為 3 的一列在兩個畫面都顯示 3；前一輪推論兩個畫面分別顯示 1／3 不適用於目前完整前端流程，予以更正。
- 剩餘差異屬 API 契約與例外情境：後端回傳列數仍與前端算法不同；前端 `quantity || 1` 將 0 當 1；新增表單倉庫讀取失敗時以空陣列計為 0。這些不能直接當作使用者已遇到的正常流程問題。
- 2026-09-18 已確認：目前魔法物品數以倉庫內永久魔法物品的 `quantity` 加總，不包含消耗品；倉庫新增、修改數量、刪除及冒險戰利品同步均更新 character 的目前數量，character-shell 讀取該值。冒險歷史結算維持當時快照，不因後續倉庫異動改寫。
- 現行來源區分：`magicItemsChange` 表示冒險中變化；休整期活動各自填寫魔法物品變化，再加總為 `magicItemsDowntimeChange`。具名的 `adventure_gained_item` 目前只關聯整筆冒險，休整期活動只有文字描述，尚無物品來源關聯。因此不能把所有數字強制改由現有「冒險獲得物品」清單產生，否則會把休整期取得誤列為冒險取得。
- 2026-09-18 已確認補充：保留冒險與休整期兩種數字來源，物品明細不強制完整。正數變化若大於同來源已填的永久物品明細，系統自動建立足以補足差額的「未命名魔法物品」，並標記來源為冒險或休整期；玩家可日後在倉庫補資料。負數變化正常保存歷史，但不得猜測或自動刪除倉庫物品；成功後提示減少件數並提供前往倉庫入口，由玩家自行刪除。冒險頁不新增物品選取清單。
- 玩家實際刪除倉庫物品後，character 與 character-shell 才更新；忽略提示時兩者維持原倉庫數量。修改或刪除負數歷史紀錄不得自動補回任何物品，因系統未記錄當初由玩家手動移除的是哪一件。
- 2026-09-18 已確認補充：若同來源具名永久物品明細數量大於玩家填寫的正數變化，系統自動將該來源的變化數字提高至明細數量並重新計算結算，不阻止送出，也不為這些已具名物品建立待補項目。
- 後端 `COUNT(*)` 與 `quantity` 加總的契約差異須於實作時統一。待設計項目：以穩定欄位標記待補物品及 `ADVENTURE`／`DOWNTIME` 來源，不依名稱或自由文字猜測；既有資料如何回填目前數量。

### C03 — 第一筆冒險的預設值

- 文件：SRS §3.3、TC-012 要求全部 null；US-005 要求開卡資料或 0。
- 程式：`getDefaults` 使用角色職業等級、金幣 0、天數 0，魔法物品另取倉庫。
- 2026-09-18 已確認：開卡指建立角色；角色起始金幣及休整期為 0，等級與職業依開卡設定。每筆冒險只輸入變化，當前值累加所有變化；詳情保存當下初始、變化及總計，不把快照再次累加。魔法物品明細另標示冒險或休整期來源。
- 舊資料沒有初始／變化／總計欄位時的回填方式，以及如何由舊起始／結束值轉成變化值，留待設計階段處理。

### C04 — 補登、修改日期、刪除舊紀錄是否重算歷史

- 文件：規定以 `play_date + created_at` 找最後一筆，卻未定義插入較早日期或更改日期後如何同步。
- 程式：新增一筆就覆寫角色職業；更新前以舊日期判斷是否最新；刪除後取最新剩餘職業。可能使角色職業和依遊玩日期讀出的等級不一致。
- 2026-09-18 使用者實測：先登錄 9/18 冒險由 1 等升至 5 等，再補登 9/1 冒險升 1 等，補登紀錄結束等級為 6，但 character-shell 當前等級仍為 5。這是使用者回報，非 Agent 自行操作的結果。
- 原因核對：新增表單初始化從 `getDefaults` 讀日期最新紀錄的結束等級 5，改遊玩日期沒有重取該日期之前的狀態，因此升 1 等後送出 6。後端 `createEntry` 依新提交職業字串覆寫角色，不判斷這筆遊玩日期是否最新；HUD 的 `parseTotalLevel` 優先讀 defaults，而其查詢仍取 `play_date DESC, created_at DESC` 的 9/18 紀錄，故顯示 5。不是單純未刷新，也不能只改 HUD 為 6 就認定修正完成。
- 額外靜態推論：角色職業文字讀 `currentClassesString`，故可能已是合計 6 等，而同頁等級仍為 5；此職業文字差異尚未由使用者實測回報。
- 2026-09-18 使用者補充：補登 9/1 時起始等級鎖為 5，無法如實填寫歷史起始狀態。
- 已確認：按遊玩日期之前最近一筆冒險或開卡狀態計算該筆初始值；玩家只輸入變化；改日期前保護已填變化，不直接覆蓋。補登後保留後續快照，若前後狀態不連續則提示，不自動重算。
- 規格案例：開卡 1 等、既有 9/18 等級變化 +4，補登 9/1 輸入 +1；新紀錄保存初始 1、變化 +1、總計 2，9/18 快照不變；當前角色累計為 6 等。
- 2026-09-18 已確認當前狀態：等級及各職業以開卡基準加上所有冒險變化；金幣與休整期由 0 加上所有冒險及活動變化。不因遊玩日期排除補登，也不要求玩家選擇是否計入。
- 修改冒險只套用新舊變化差額，成功後重存相同內容不重複計入；只改日期、名稱或備註不影響當前值。刪除冒險撤回該筆變化，不重算其他歷史快照。
- character-shell 統一讀取 `character` 保存的當前等級／職業／金幣／休整期，不再混用日期最新冒險的結束值。日期排序只服務歷史呈現及已確認的預填規則，不決定當前累計值。
- 實作差距：版本庫 V6 migration 已定義 `current_gold`、`current_downtime` 等欄位，但目前 `Character` entity 未映射這兩欄，也未保存獨立開卡基準；未連線確認實際資料庫是否已有欄位。欄位存在不表示上述算法已實作。技術保存方式與舊資料回填須另行設計，不直接用現有當前值當基準再加歷史，以免重複計算。
- 規格位置：`character-management` 的基準、累計及一致來源要求，`adventure-log` 的貢獻更正，以及 `state-consistency` 的角色更新回滾情境。均為已確認目標，尚未修改產品程式。
- 其他待決策：同日排序、無日期歷史資料、開卡狀態保存方式、等級／職業扣回後不合法及舊資料基準。尚未確認金幣等其他資源的補登預填策略。

### C05 — 玩家名稱是會員暱稱還是角色欄位

- 文件：US-001／003 必填可編輯；SRS §2.1.1 與 DB Migration 13 說由會員暱稱提供。
- 程式：角色表單沒有玩家名稱，DTO／Service／Mapper 仍保留 `playerName` 欄位。
- 建議：會員顯示名稱為唯一來源，角色舊欄位僅為兼容資料；需確認 API 與舊資料如何呈現，而非只移除表單必填。

### C06 — 職業／等級的表示及驗證責任

- 文件：SRS 使用 `classLevels` 陣列、甚至要求更新已移除的 `character_class_level` 表；DB 與程式已用 `currentClassesString`。
- 文件要求至少一個職業、13 選項、職業總和等於結束等級；角色編輯只能改基本資料。
- 程式：後端角色 DTO 未強制職業必填，角色更新 API 仍可改職業字串，冒險後端未驗算職業等級總和。前端另有 1–20 範圍。
- 建議：先確認規則與兼容性，不把格式重構混入此次基線；後端亦應執行確定後的規則。需討論開卡是否僅限 1／5／11 級、重複職業、自訂職業與總等級上限。

### C07 — 遊玩日期及空白子項目

- 文件：SRS §2.2.1／US-005 日期必填；TC-003 卻要求所有欄位空白也成功。活動描述在欄位表選填，§9.4 又禁止空白卡片。
- 程式：冒險前端日期必填，後端主 DTO 的日期沒有必填註解；完整儲存子 DTO 有驗證。
- 建議：主表日期必填，其他選填；可以不新增任何活動，但新增的活動描述不可空白。補寫前後端一致的拒絕案例及舊 null 日期策略。

### C08 — 休整期活動是純文字還是計算輸入

- 文件：US-005b 要求純文字且不影響計算；README／SRS 描述較接近手動資源記錄。
- 程式：活動有金幣、天數、物品欄位，`recalculateDowntimeTotals` 會覆寫主表休整期變化，儲存後編入描述文字，再以格式解析。
- 待決策：活動數值是結算唯一來源，還是可以和手動填寫的主表差額並存。須處理刪除活動、無法解析的舊文字及雙重計算。

### C09 — 迎頭趕上升級與休整期扣款

- 文件：README／US-005 要求花費休整期換取升級，未明確標示適用 AL 規則版本及費率。
- 程式：升級開關／次數直接增加結束等級；另有「迎頭趕上」活動預設 -10 天，開關與扣款非同一計算來源。
- 待決策：手動紀錄或自動扣款、每級費用、可用級別、天數不足時的拒絕規則；不憑記憶宣稱為官方 AL 規則。確認版本後若需引用遊戲規則再核對官方文件。

### C10 — 快捷消耗及歸零行為

- 文件：SRS §2.3.2 說用盡自動移除；US-016b 說先確認是否移除。
- 程式：目前倉庫列表未找到快捷扣除方法或實際使用按鈕；可透過編輯改數量。
- 待決策：是否保留快捷消耗需求；數量 0 時自動移除或保留／詢問。不得因文件寫「已實作」就標為完成。

### C11 — OAuth 合併帳號與帳號停用

- 文件：US-AUTH-003 同 Email 自動登入既有帳號；帳號模型存在 `is_active`，未完整描述各登入途徑。
- 程式：密碼登入檢查停用，OAuth 路徑沒有同等檢查；Google 驗證欄位缺省被視為通過，Discord 路徑未見 Email 驗證狀態檢查便依 Email 綁定。
- 建議：任何登入途徑均尊重停用狀態；第三方 Email 合併需明確已驗證，或要求登入原帳號後綁定。需要使用者選擇綁定 UX；安全缺口另列實作任務，不能把現況描述成安全保證。

### C12 — 離線資料與跨帳號隔離

- 文件：SRS §6、US-018、README 承諾已載入角色／冒險可離線瀏覽。
- 設定：`frontend/ngsw-config.json` 只有靜態資產／字型快取，没有 API `dataGroups`。記憶體快取不等於離線重開仍可讀。
- 待決策：離線僅開啟 App 外殼，或保留私人角色資料供離線讀取。若保留，需定義帳號隔離、登出清除、到期、更新、離線寫入提示；不得直接快取所有認證 API。

### C13 — 部署與 migration 文件

- 文件：DB 手冊要求手動逐段 SQL；舊部署計畫和 AGENTS 要求 JPA。現況為 MyBatis、Flyway，自動 `migrate` 且 baseline 為 0。
- 發現：Flyway V3 包含清空資料與固定預設帳號；與把現有資料庫直接納入 migration 管理存在資料保留風險。本紀錄不抄錄憑證內容。
- 建議：將手工 SQL／舊 JPA 計畫標為歷史；另行制定現有資料庫接管流程、備份與 migration history 驗證。不可直接在既有正式資料庫試跑，也不可隨意修改已套用 migration 造成 checksum 分歧。

### C14 — 排序、搜尋及路由

- 文件：冒險時間軸由舊到新；早期計畫包含日期搜尋；路由 `/log`。
- 程式：列表預設由新到舊，可切換遊玩／建立時間並記憶；搜尋名稱／代碼／DM／故事獎勵；路由 `/adventures`。倉庫另有稀有度排序、搜尋及同調篩選。
- 建議：路由與支援的互動依現況記錄，預設排序及是否新增日期搜尋再確認。API 升序與 UI 可排序並非本身矛盾，勿混為同一需求。

### C15 — 稀有度、數量與同調

- 文件：US-014 僅五種稀有度，SRS 包含 ARTIFACT；SRS 物品欄位未完整列出同調，API 又限定永久物品適用。
- 現況：前端與 Flyway V17 支援 ARTIFACT；數量／型別在快照與倉庫同步時有差異（C01）。
- 建議：採 SRS 六種稀有度；同調僅永久物品。待補数量正整數／0 的規則、變更物品類型時的同步策略與 API 驗證。

### C16 — 法律聲明入口

- 文件：US-LEG-001 要求移除多餘連結，US-LEG-002 卻指定頁腳有「版權與免責聲明」入口；SRS 稱全域彈窗但未完整指定入口。
- 建議：保留一個明確條款入口，且彈窗關閉後保留未送出的表單。既有條款的法律效果不由本次文件整理認定。

### C17 — 非功能性需求無量測條件

- 文件：載入 <3 秒、可用率 >99%，並將服務商免費層與 SLA 並列；沒有裝置／網路／資料量／冷啟動／統計期間。
- 建議：先由使用者確認量測環境與目標，再形成可驗收規格。現階段不將未核實的第三方 SLA 當作保證。

## 有明確新文件依據的整理候選

- 早期「無登入 MVP」與現行會員隔離規格時間階段不同；早期計畫標為歷史，無需恢復無登入架構。
- SRS 2026-09-17 補充已明示優先於舊同步描述，因此完整儲存的清單非 null、載入失敗防護、快取失效等直接提取為草案。
- 資源合計在 SRS §2.2.1、README 與程式一致為三項相加；TC-008–010 的兩項公式應更新，不能遺漏休整期變化。
- ARTIFACT 在新 SRS、前端及 V17 一致存在；舊 User Story 枚舉不完整。
- `backlog.md` 和架構規範缺檔需修正引用／補建有來源的新文件；不虛構舊任務完成紀錄。

## 新增實作發現：職業識別值須與中文標籤分離

- 使用者以「野蠻人」開卡後，冒險表單選項值為「野蠻人 (Barbarian)」，造成第一筆升級時被識別成移除原職業。已確認資料層與 API 應統一以英文標準 key 儲存，前端獨立顯示中文名稱；`Warlock` 的預設顯示譯名採「契術師」，中文別名「邪術士」亦正規化為相同英文 key。
- 已新增 V21 migration（將舊值轉成 key，並以 CHECK constraint 阻止非標準值留存）與前後端正規化／顯示程式；本項回歸測試與 migration 執行驗證尚未完成。OpenSpec task 1.4、2.7、4.7 保持未勾選。原手動驗收 task 5.5 亦仍待完整操作驗收。

## 2026-09-20 正式環境 migration 與真人驗收

- 使用者已於 Supabase 確認 Flyway V24 `V24__require_character_opening_classes.sql` 套用成功，`success = true`。
- 使用者已完成本次變更所需的真人驗收，確認開卡基準與第一筆冒險的目前等級一致，並確認本輪驗收流程正常；task 5.5 已完成。

## 2026-09-20 職業英文識別值回歸驗證

- PostgreSQL 整合測試確認 V21 將角色開卡／目前職業與冒險起始／結束快照的既有「戰士 (Fighter)」字串正規化為 `Fighter`；V22 拒絕未知 `Homebrew1` 識別值。
- 服務層回歸測試確認「野蠻人 1 等」的第一筆冒險可增加 1 級並保存為 `Barbarian1` → `Barbarian2`；中英混合快照在貢獻計算中合併為同一個 `Barbarian`，不支援的職業名稱在儲存前被拒絕。
- 前端共用職業對照測試確認角色／冒險表單由中文與舊格式輸入產生英文識別值，角色列表與冒險詳情可將英文或舊中文快照顯示為中文；`Warlock` 的 API key 維持不變，預設標籤為「契術師」。前端測試與 production build 成功；既有 SCSS bundle budget warning 沒有新增。

## 完成條件

1. 每份来源中的有效業務規則、API 契約、欄位約束及非功能性要求均對應到規格，或有明確歷史／延期理由。
2. C01–C17 各有使用者決策或明確確認的排除範圍；新增衝突亦納入追蹤。
3. 決策附正向、失敗及邊界案例；實作不符合規格之處另列，不能以文件通過取代程式修正。
4. 更新文件入口及舊敘述，通過 OpenSpec 驗證後才同步正式規格。
5. 若進入產品總翻新實作，依已定稿能力逐項完成及驗收；本次草案不代表產品翻新完成。
