# Tasks

## 1. 資料庫與持久層

- [x] 1.1 新增附加式 Flyway migration，建立 `character.initial_classes_string`、`adventure_entry.recording_model_version`，以及物品表的 nullable `acquisition_source` 與 `needs_details`；回填版本 1、可確認的 `ADVENTURE` 來源，其餘來源保持 null，並以測試資料庫成功執行 migration 驗證
- [x] 1.2 將 character 既有 `current_gold`、`current_downtime`、`current_magic_items` 與新增 initial classes 映射至 entity、mapper 及 response DTO；由建立服務初始化、一般角色 request 不得覆寫目前資源，並以 mapper/service 測試驗證新增、讀取及更新值一致
- [x] 1.3 將 adventure model version、物品 acquisition source 與 needs-details 映射至 entity、mapper 與 response DTO，不開放一般物品 request 任意指定系統來源；並以持久層測試驗證 `ADVENTURE`、`DOWNTIME`、null 及待補狀態可正確往返
- [x] 1.4 新增 V21 migration，將既有角色開卡／目前職業與冒險起始／結束快照中的已知中英文名稱正規化為英文識別值，並以測試資料庫驗證舊字串轉換及未知內容處理
- [x] 1.5 新增 V23 migration，為角色加入非負 `initial_gold` 與 `initial_downtime`（既有角色預設為 0），並以 PostgreSQL 持久層整合測試驗證升級與預設值
- [x] 1.6 新增 V24 migration，要求 `character.initial_classes_string` 非 null 且非空白，不猜測或覆寫既有開卡基準，並以 PostgreSQL 整合測試驗證 null／空白寫入會被拒絕

## 2. 後端變化式帳本

- [x] 2.1 將冒險儲存 request 改為只接收等級／職業、金幣、休整期與魔法物品變化，拒絕由客戶端覆寫初始或總計，並以 DTO 驗證測試確認非法欄位與不合法等級變化會被拒絕
- [x] 2.2 實作 `play_date, created_at, id` 的穩定前置快照查詢與初始／變化／總計計算，並以服務測試覆蓋有前置紀錄、無前置紀錄、補登、同日紀錄及 legacy null 日期
- [x] 2.3 讓新增版本 2 冒險把變化加入 character，編輯只套用新舊差額，刪除只撤回最後保存的變化；版本 1 保留舊基準相容分支，並以服務測試驗證重複儲存不重複累計、只改日期不改當前值及全部刪除回到開卡狀態
- [x] 2.4 將等級／職業變化限制為非負整數、職業增加合計等於總等級變化且結果不超過 20，並以單元測試驗證單職、多職、降低既有正變化及各種拒絕情境
- [x] 2.5 讓補登或改日期只重算被操作紀錄的快照、不改寫後續快照，並回傳不連續提示資訊；以服務測試驗證後續快照保持原值而 character 累計包含補登變化
- [x] 2.6 確保冒險主表、活動、戰利品、倉庫與 character 更新共用交易，並以失敗注入測試驗證任一步驟失敗時全部回滾
- [x] 2.7 正規化冒險職業變化輸入、快照解析及 character 貢獻計算為英文職業識別值，拒絕不支援的新職業名稱，並以回歸測試覆蓋 `野蠻人1` 開卡後升至 2 級與舊中英混合字串
- [x] 2.8 讓無日期前置冒險的快照以開卡職業、金幣、休整期與未關聯冒險的永久倉庫物品為初始值；提供唯讀開卡修正預覽，並在確認儲存時以所有冒險變化重算 character、保留歷史快照，以服務測試覆蓋既有紀錄及預覽不寫入

## 3. 魔法物品與歷史戰利品

- [x] 3.1 儲存冒險與休整期物品時分別寫入 `ADVENTURE`、`DOWNTIME`，手動或無法判斷來源的物品保持 null，並以服務測試驗證不依名稱或備註猜測來源
- [x] 3.2 正數魔法物品變化缺少同來源明細時建立 `needs_details = true` 的待補永久物品；具名明細較多時自動提高變化值，並以服務測試驗證兩種來源、部分缺漏及明細超額情境
- [x] 3.3 負數魔法物品變化只保存歷史與回傳前往倉庫提示，不自動刪除或補回物品，並以服務測試驗證修改及刪除負數歷史紀錄不改動倉庫
- [x] 3.4 修正既有戰利品同步，使數量不變不補貨、增加只加入差額、減少最低扣至 0，並以使用者回報的「刪除 3 瓶後只改備註」及 3→5、5→1 案例建立回歸測試
- [x] 3.5 每次永久物品新增、修改、刪除或戰利品同步後重新加總 `quantity` 至 character，並以測試驗證消耗品不計入、交易失敗會回滾

## 4. 前端表單與顯示

- [x] 4.1 更新 Angular models 與 adventure service 契約，只傳送變化值並接收伺服器計算的初始／變化／總計及提示資訊，以型別檢查與服務測試驗證 payload 不再包含可覆寫快照欄位
- [x] 4.2 將冒險表單改為輸入本次變化，資源區不顯示初始值、只即時呈現變化與結算總計；以 Signals 保留計算與負值驗證，詳情頁保留初始／變化／總計，並以元件測試驗證計算及錯誤提示
- [x] 4.3 將冒險詳情改為紙本帳本格式呈現各資源的初始、變化與總計，並以元件測試驗證正數、負數、小數金幣及補登快照
- [x] 4.4 在冒險詳情與倉庫為非空 acquisition source 顯示「冒險獲得」或「休整期獲得」，null 不顯示來源標籤；待補物品顯示可辨識狀態，並以元件測試驗證三種呈現
- [x] 4.5 儲存負數魔法物品變化後顯示減少件數與前往倉庫入口，不在冒險表單加入倉庫選取清單，並以元件測試驗證提示與導覽
- [x] 4.6 讓 character-shell 只讀 character 目前值，並在冒險或倉庫異動成功後刷新；以元件／整合測試驗證補登後顯示累計值而非最新日期快照
- [x] 4.7 角色及冒險表單以英文識別值提交職業、以前端中文對照表顯示職業名稱，詳情與角色列表正確轉譯新舊快照，並以元件測試驗證 `Warlock` 顯示名稱可獨立調整
- [x] 4.8 在 character-shell 的編輯角色入口提供開卡職業／等級、金幣及休整期基準編輯；有冒險時先顯示後端預覽並要求確認，取消不寫入，開卡物品引導至倉庫管理，並以元件測試驗證流程與提示

## 5. 舊資料與整合驗證

- [x] 5.1 建立唯讀的一致性檢查查詢或管理腳本，列出無法解析的舊職業字串、來源不明物品及 character／倉庫魔法物品數差異，並以測試資料驗證報表不會修改資料
- [x] 5.2 更新 `database-schema.md` 與相關需求文件，說明新增欄位、版本 1／2 相容規則及 nullable 來源語義，並以文件搜尋確認不再宣稱所有物品必有四種來源
- [x] 5.3 執行後端完整測試與打包，確認 `./mvnw test` 及 `./mvnw clean package -DskipTests` 成功
- [x] 5.4 執行前端測試與 production build，確認測試命令及 `npm run build` 成功且沒有新增 bundle budget error
- [x] 5.5 手動驗收開卡、一般新增、9/1 補登、修改差額、刪除、正負魔法物品、待補物品與兩種來源標籤流程，將結果記錄於 change review，並確認手機版沒有水平溢出
- [x] 5.6 更新資料庫綱要、SRS 與 User Stories，描述可修正開卡基準、初始資源、倉庫開卡物品及歷史快照不變規則
